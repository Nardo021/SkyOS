/**
 * @deprecated UI moved to shadcn in apps/desktop — import from `@/components/ui/*`.
 * This package is kept for legacy references only.
 */
export {
  Icon,
  type IconProps,
  type TablerIcon,
  type TablerIconProps,
} from "./Icon";
